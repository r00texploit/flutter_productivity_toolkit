name: flutter_productivity_toolkit
description: A comprehensive Flutter developer productivity toolkit that provides unified solutions for state management, navigation, testing utilities, and development workflow optimization.
version: 0.1.8
homepage: https://pub.dev/packages/flutter_productivity_toolkit
repository: https://github.com/r00texploit/flutter_productivity_toolkit
documentation: https://pub.dev/documentation/flutter_productivity_toolkit/latest/

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  analyzer: '>=7.0.0 <10.0.0'
  build: '>=3.1.0 <5.0.0'
  collection: ^1.17.2
  faker: ^2.1.0
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  meta: ^1.16.0
  path: ^1.8.3
  source_gen: '>=3.1.0 <5.0.0'

dev_dependencies:
  build_runner: ^2.7.1
  build_test: ^3.3.3
  flutter_lints: ^6.0.0
  mockito: ^5.5.0
  test: ^1.25.15

flutter:
  # This package is a Dart package, not a Flutter plugin
  # No platform-specific code is included