name: flutter_productivity_toolkit
description: A comprehensive Flutter developer productivity toolkit that provides unified solutions for state management, navigation, testing utilities, and development workflow optimization.
version: 0.1.0
homepage: https://github.com/r00texploit/flutter-productivity-toolkit
repository: https://github.com/r00texploit/flutter-productivity-toolkit
issue_tracker: https://github.com/r00texploit/flutter-productivity-toolkit/issues

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  analyzer: ^6.2.0
  build: ^2.4.1
  collection: ^1.17.2
  faker: ^2.1.0
  flutter:
    sdk: flutter
  meta: ^1.9.1
  path: ^1.8.3
  source_gen: ^1.4.0

dev_dependencies:
  build_resolvers: ^2.4.1
  build_runner: ^2.4.7
  build_test: ^2.2.1
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter
  mockito: ^5.4.2
  test: ^1.24.6

flutter:
  # This package is a Dart package, not a Flutter plugin
  # No platform-specific code is included